name: new-yaml-pipeline

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Build
        run: |
          echo "Running build step"
          # Add your actual build commands here, e.g., running a script or compiling code